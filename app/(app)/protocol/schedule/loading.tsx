import { ProtocolSubpageLoading } from "@/components/feel/RouteSkeletons"

/** The page's shell while it loads, so the card tap switches at once. */
export default function Loading() {
  return <ProtocolSubpageLoading title="Schedule" />
}
