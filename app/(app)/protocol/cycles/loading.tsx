import { ProtocolSubpageLoading } from "@/components/feel/RouteSkeletons"

/** The page's shell while it loads, so the tile tap switches at once. */
export default function Loading() {
  return <ProtocolSubpageLoading title="Cycles" />
}
